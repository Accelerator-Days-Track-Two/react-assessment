import React from 'react';
import psyduck from '../assets/psyduck.png'


const SearchBar = () => {


    const handleChange = () => {
    }

    const fetchPokemon = () => {
    }

    return (
        <nav>
            <form>
                Search Bar
            </form>
        </nav>
    );
};

export default SearchBar;