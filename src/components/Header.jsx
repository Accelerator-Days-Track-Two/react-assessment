import React from 'react';
import pokemonLogo from '../assets/pokemon-logo.png'

const Header = () => {
    return (
        <header>
            Header
        </header>
    );
};

export default Header;